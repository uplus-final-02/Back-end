# Back-end
# 멘토링 질문 목록

## 콘텐츠 - 추천 시스템

- 추천 알고리즘 직접 작성 vs 기존 알고리즘 활용
    - API 통신 방식으로 독립 서버 운영 고려 중
    - 추천 알고리즘 하나를 위한 독립 서버가 적절한가?
    - Process Builder 방식은 성능이 좋지 않음

---

## Elasticsearch

- 동기화 주기를 30초로 설정하는 것이 적절한 방법인지?

---

## 플레이어 - 시청 이력 저장 설계

- 시청 지점(savepoint) 저장 빈도에 따른 DB 부하 우려
    - 현재 구조: 10초마다 프론트에서 API 호출 → 이탈 시 DB 업데이트
    - Redis 캐싱 후 이탈 시 DB 반영 방식이 나은지?
    - 북마크도 동일한 설계 적용 여부
        - Redis 버퍼링 후 배치로 DB 반영?
        - 낙관적 락(Optimistic Lock) 적용?

---

## 조회수 처리

- 엔티티에서 조회수를 직접 `++` 처리 시 동시성 문제 없는지?

---

## 북마크 조회 쿼리

- 북마크 조회 시 쿼리를 2번으로 나눠서 처리하는 현재 구조에서 `IN` vs `JOIN FETCH` 중 어느 방식이 나은지?

---

## 회원 인증

- 인증 구현 방식에 대한 피드백 (현재 방식이 적절한지?)

---

## API 서버 구조

- `user-api`와 `admin-api`가 같은 DB를 공유하는 것이 괜찮은지?
    - 도메인 분리가 필요한지?
    - admin에서 user 조회는 어떻게 처리해야 하는지?
    - 공통 데이터는 공유하고, 각자의 데이터는 독립적으로 관리하는 구조가 맞는지?

---

## 서비스 간 의존성

- `Content` 컨트롤러에 `BookmarkService`를 직접 주입하는 현재 구조가 괜찮은지?
    - Facade 패턴 도입을 고려해야 하는지?

---

## 향후 기능 추가

- 추가로 구현하면 좋을 기능 추천 (방향성 조언 요청)